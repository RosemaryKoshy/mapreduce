"you'll have to implement at least one original test of your own"
    add distributed grep 'dgrep' test
        The map function emits a line if itmatches a supplied pattern.
        The reduce function is anidentity function that just copies
        the supplied intermediate data to the output.
